defaults:
  - datamodule
  - module
  - trainer
  - hydra
  - _self_

seed: null
checkpoint: null
find_initial_lr: true

datamodule:
  config:
    dataset_dirpath:
      _args_: ["data/kaggle_3m"]

trainer:
  logger:
    experiment: tumorsemseg_v0
