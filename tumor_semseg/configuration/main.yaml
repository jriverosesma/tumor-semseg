defaults:
  - datamodule
  - module
  - trainer
  - hydra
  - _self_

seed: null

datamodule:
  config:
    dataset_dirpath:
      _args_: ["data/kaggle_3m"]

trainer:
  logger:
    experiment: tumorsemseg_v1
